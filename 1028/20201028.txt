소켓

메소드 동기화
임계영역(Critical Section) : 둘 이상의 스레드가 동시에 쓸수 없는 영역

해당 함수에 잠금장치를 넣어줄 수 있음
>> syncronized

임계영역의 범위문제 -- 중요
1. 범위를 두고 동기화

	synchronized(this){ // this는 블럭 영역이 전부가 됨. this 말고 그 안에 다른 것들을 사용하면 그것이 동기화 대상이 됨
	}
2. 입력과 조회를 분리해서 구현