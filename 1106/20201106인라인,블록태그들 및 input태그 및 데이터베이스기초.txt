헤더나 푸터는 section을 사용하지 않는다.

section의 3가지 > article, aside, nav

--블록들을 만드는 태그들...

블록태그와 인라인태그
블록 : 무언가를 담을 상자를 만드는것
	기본값 너비 100% 높이 컨텐츠 크기


인라인 : 컨텐츠를 구분지을 목적으로 사용. 영역이 없음.
	너비나, 높이 지정 불가능
	1. 구분해서 태그만으로 스타일을 줄수 있음
	2. semantic한 의미를 갖고있는 태그들이 있음
	datalist > 자동완성기능 등에 사용할 데이터들을 담아두는 태그(input태그의 입력 범주를 좁혀주기위해 사용)
	del > 상품 할인시에 가격에 줄긋는 용도
	kbd > 키보드 키라는 것을 말하고싶을때
	ruby > 발음기호표현
	iframe > 파일안에 다른 파일을 끼워넣을때 사용
	ins > 추가된 내용

	input태그 > 다양한 type 존재... html5에서 새로이 적용된 type들은 사용하지 않는게 좋음..(브라우저마다 표현방식이 다 다름)

자바스크립트 -> form태그의 유효성을 검사하여 제출될수 있도록 하기 위해서 만들어짐
but, html5가 되면서 입력값이 없으면 제출할 수 없도록 하는 자체기능이 생김
<input placeholder="20자 이내의 제목을 입력하세요."> placeholder input 내용에 대한 설명을 넣어주고싶을때(포커싱을 주면 사라짐)
페이지가 처음 시작되자마자 focus를 주는법 >> 속성에 autofocus 넣어줌
html > autofocus
xhtml > autofocus="autofocus"
<input name="key"> name은 인풋태그에서 전달하는 값을 저장하는 key
key를 통해 데이터를 전송하고나면 그 기록이 남음.(자동완성으로 사용됨) >> 그 기능을 켜고 끄기 위한 속성으로 autocomplete="on/off"

<input autocomplete="off" name="title" style="width: 400px;" 
	placeholder="좋아하는 동물의 종류를 입력하시오." autofocus="autofocus" 
	list="type">
                                <datalist id="type">
                                    <option value="Dog">
                                    <option value="Cat">
                                    <option value="Duck">
                                </datalist>
input 태그의 범주를 적용해주기 위해 list 적용. datalist의 id값을 가져다 씀

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

html5 유효성 검사 속성
>> required
>> type="number" 숫자만 입력가능 + min="", max="" 로 숫자 범위 지정가능
>> 이메일,전화번호,주민등록번호등의 형식이 맞는지 검사하기 위해 pattern=""지정 가능 정규표현식!!! 중요
	pattern은 문자가 입력값에 포함되어있는지를 검사함.
	시작과 끝이 완전히 일치하도록 하려면? >> 입력의 시작과 대응 ^ , 입력의 끝과 대응 $ 

-----------------------------------------------------------------------------------------------------------------------------------------------

DBMS
각기 따로 쓰던 데이터들을 모아둔 것 : 데이터베이스
데이터베이스의 데이터는 다 찢어져 있음 : 무결성을 지키기 위해(결함을 없애기위해) 중복되는 것들을 제거함
데이터베이스 사용을 위해 동시성, 성능, 보안을 관리할 수 있는 MS가 필요