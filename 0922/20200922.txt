네트워크 소켓

LAN(locan area network) 
버스형 : 회선 절약(비용 절약), 전용선이 없고 공유선만 있어서 혼잡제어가 필요.
링형 : 토큰을 가진 사람만 데이터 전송이 가능. 
스타형 : 스위치 사용. 요즘 많이 사용. 비용은 비쌈. 하지만 동시에 여러 통신이 가능

데이터 처리방법(이게 내 데이터인지 아닌지?)
MAC Controller 가 판단을 해줌. 
모뎀 : 변복조기(아날로그 - 디지털)

공유라인 사용시 CSMA/CD로 공유라인이 사용중인지 체크하고 안사용중일때 전송.
두개 이상의 컴퓨터가 동시에 공유라인으로 데이터 전송시 충돌 발생(collision)
이 문제를 해결하기 위해 collision detection하고, 버퍼에 전송 데이터를 전송이 완료될때 까지 보관

그럼 이런 여러 컴퓨터가 연결된 것들을 전세계적으로 여러 네트워크를 연결하려면 어떡하나?
inter network 개념 생김. internet
각 컴퓨터에 접근하기 위한 주소 필요 > ip 주소.
but 주소 부족현상으로 IPv6 개발

192.168.0.3 중에 192.168.0 은 네트워크 주소. 마지막 3은 내 ip번호.
마지막에 255는 브로드캐스팅용 주소.
MAC add를 모를때는 ip번호를 arp가 모든 단말기에 뿌리면서 이거 누구냐고 물어봄
해당 ip번호를가진 단말기가 본인의 MAC add를 응답함. 
그 MAC add를 사용,

그럼 찾고자 하는 목적지가 현재 네트워크가 있는지? 아니면 다른 네트워크에 있는지 확인할수 있는
구분은 어떻게 하나?
>> 서브넷 마스크
앞에 255.255.255.0 // 앞 3자리만 비교를 해보자는것!!!
앞 3자리는 네트워크 주소를 가리키므로 이 3자리가 같으면 같은 네트워크다!

패킷 헤더에 여러가지 오류 제어정보와 경로 정보 등이 담겨 있다.

ip 주소는 건물을 가르키고(네트워크계층)
UDP(TCP) 에서는 정확한 호수(사서함 번호, 포트 번호)를 정해주는 역할을 한다.(전송 계층)

TCP는 속도가 느린 대신 각 클라이언트 마다 따로 버퍼가 있어서 손실없고 안전한 데이터 전송 제공.
UDP는 어떠한 검사도 하지 않음. 그래서 속도가 빠름. 하지만 신뢰성이 떨어짐
